version: 2.1

orbs:
  freegle: freegle/tests@1

workflows:
  pr-tests:
    jobs:
      - freegle/php-tests:
          filters:
            branches:
              ignore:
                - master
                - main
